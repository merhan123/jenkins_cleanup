pipeline {
    agent any
    stages {
        stage('Configure Docker Image Properties') {
            steps {
                echo 'Scaffold: configure registry, credentials, and retention policy.'
            }
        }
        stage('Preview Docker Image Cleanup') {
            steps {
                echo 'Scaffold only: no images are deleted. Implement a dry run first.'
            }
        }
    }
}
